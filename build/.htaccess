<IfModule mod_rewrite.c>
	RewriteEngine On

	# If file or folder exists: serve it normally
	RewriteCond %{REQUEST_FILENAME} -f [OR]
	RewriteCond %{REQUEST_FILENAME} -d
	RewriteRule ^ - [L]

	# Otherwise always serve the SPA entry
	RewriteRule ^ index.html [L]
</IfModule>
